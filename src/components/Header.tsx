import React from "react";

type Props = {};

export const Header = (props: Props) => {
  return (
    <header className="absolute top-0 flex w-full justify-center py-4">
      <h1 className="text-4xl font-bold text-white">typle.</h1>
    </header>
  );
};
